<%= turbo_frame_tag 'packagings_modal_form' do %>
  <%= form_with model: @packaging, 
                      defaults: { input_html: { class: 'mb-2' } },
                      data: {
                        action: 'oroshi--dashboard#modalFormSubmit:prevent',
                        refresh_target: 'packagings'
                      } do |form| %>
    <%= render 'form', form: form %>
    <%= form.submit t('common.buttons.save'),
                    class: 'btn btn-primary my-2' %>
  <% end %>
<% end %>