# Database Configuration
POSTGRES_USER=oroshi
POSTGRES_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

# Oroshi Configuration
OROSHI_DOMAIN=localhost

# Rails Environment
RAILS_ENV=development
RAILS_MAX_THREADS=5

# Optional: Active Storage (for file uploads)
# GCLOUD_PROJECT=your-project-id
# GCLOUD_BUCKET=your-bucket-name
# GCS_CREDENTIALS_FILE=path/to/credentials.json

# Optional: Email delivery (Resend)
# RESEND_API_KEY=re_your_api_key
# MAIL_SENDER=noreply@yourdomain.com

# Optional: Rails secrets (auto-generated if not set)
# SECRET_KEY_BASE=
# RAILS_MASTER_KEY=
