<%= turbo_frame_tag 'buyer',
                    class: 'card' do %>
  <%= form_with model: @buyer,
                        class: 'rounded p-2',
                        data: {
                          controller: 'oroshi--supply-settings-form'
                        } do |form| %>
    <%= render partial: 'form', locals: { form: form } %>
  <% end %>
<% end %>