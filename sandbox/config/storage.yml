test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

google:
  service: GCS
  project: <%= ENV["GCLOUD_PROJECT"] %>
  credentials: <%= ENV["GCS_CREDENTIALS_FILE"] %>
  bucket: <%= ENV["GCLOUD_BUCKET"] %>
